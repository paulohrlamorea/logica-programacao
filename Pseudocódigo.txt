{
Início

	inteiro numeroTab, termo, contador 

	numeroTab <- 0 
	termo <- 0
	contador <- 1 	

	escreva(Olá Jovem! Vamos calcular!)
	escreva(Eu sou bom em tabuadas!)

	leia(Digite o número que deseja tabular:)
	
	enquanto(contador <= 10){
		contador <- contador++
		termo <- termo + numeroTab
		escreva(termo)
	}

Fim
}